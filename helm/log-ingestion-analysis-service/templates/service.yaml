apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-ingestion-analysis-service.fullname" . }}
  labels:
    {{- include "log-ingestion-analysis-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-ingestion-analysis-service.selectorLabels" . | nindent 4 }}
